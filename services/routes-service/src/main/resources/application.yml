spring:
  application:
    name: routes-service
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: false

server:
  port: 8080

logging:
  level:
    root: INFO
    ru.ci_trainee: DEBUG

yandex:
  gpt:
    api:
      key: ""